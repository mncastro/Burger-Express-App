<div class="jumbotron"><h1>EAT-Da-Burger!</h1>
</div>

<h2>Burgers that have not been eaten</h2>

<ul>
  {{#each burgers}}
    {{#unless devour}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devour}}
      {{> burgers/cat-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>

<form class="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devour?</label><br>
    <input type="radio" name="devour" value="1" checked> Yes, devour!<br>
    <input type="radio" name="devour" value="0"> No, don't devour
  </div>

  <button type="submit">Add Burger</button>
</form>
